import { LoadingContainer, Loading } from './styled';

function Loadingpage() {
  return (
    <LoadingContainer>
      <Loading></Loading>
    </LoadingContainer>
  );
}

export default Loadingpage;
